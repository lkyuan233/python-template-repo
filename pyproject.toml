[project]
name = "python-template-repo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
[tool.uv.scripts]
test = "pytest"
dependencies = [
    "pytest>=8.3.5",
]
